channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # base
  - r-base =3.6.1
  - python >=3.7
  # render
  - reportsrender >=0.3
  # do the analysis
  - r-ggplot2
  - r-dplyr
  - r-readr


