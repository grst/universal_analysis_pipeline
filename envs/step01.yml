channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # base
  - r-base =3.6.1
  - python >=3.7
  # render
  - grst::reportsrender >=0.2.1
  # do the analysis
  - r-ggplot2
  - r-dplyr
  - r-readr
  - pip:
      - reportsrender >=0.3.0


